import styles from "./index.module.scss";

const Preview = () => {
  return (
    <div className={styles.wrapper}>
      <div className={styles.inner}>Preview</div>
    </div>
  );
};

export default Preview;
